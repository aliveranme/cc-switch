# CNB 流水线 — CC Switch
# 多平台（Linux）安装包构建
###############################################################################
# 特点：
# - PR 自动检查（类型检查 + 测试 + 编译检查）
# - push 自动构建开发版安装包
# - tag_push 构建正式发布安装包

include:
  - .ci/.pr.yml
  - .ci/.release-build.yml
